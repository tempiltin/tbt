<!DOCTYPE html>
<html lang="en">
{{> head}}

<body>

    <!-- loader Start -->
    {{> loading}}

    <div class="wrapper">
        {{#if isAuth}}
        {{> slidebar}}
        {{> navbar}}
        {{/if}}
        {{{body}}}
    </div>

    {{> footer}}
    {{> script}}

</body>

</html>